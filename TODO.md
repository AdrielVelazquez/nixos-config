Pinned exceptions to revisit:

- `nixpkgs-kernel-master` in `flake.nix`: points `razer14` at Nixpkgs PR #523596 commit `436d0dc2f2c1c949fcc575940ccf5f00b32d9424` to keep Linux 7.0.10 for the upstream MediaTek Bluetooth fix while also picking up OpenRazer 3.12.3, which fixes the Linux 7.0.10 `hid_report_raw_event` build failure. Checked 2026-05-25: current `nixos-unstable` still has linux_7_0 7.0.9, `master` has 7.0.10, and OpenRazer 3.12.3 PR #523596 remains open. Next check: remove this PR pin once the main `nixos-unstable` input has Linux 7.0.10 or newer and OpenRazer 3.12.3 or newer.
- `nixpkgs-antigravity` in `flake.nix`: follows nixpkgs `master` for `antigravity-cli` while the package is not available in the main pinned `nixpkgs` input. Recheck nixpkgs unstable before removing. Checked 2026-05-25: `master` has `antigravity-cli` 1.0.2 from upstream commit `f123c32d53beed2732137a65deb285db994c3aec`, but current `nixos-unstable` does not; upstream installs `antigravity-cli` rather than the older `agy` binary.
