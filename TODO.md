Pinned exceptions to revisit:

- `packages/headroom-ai-path-idempotent-memory-mcp.nix` is a minimal patch against Headroom 0.37.0 for `homeConfigurations.cachyos-framework13` and the embedded Home Manager user in `nixosConfigurations.razer14`. Original issue: repeated `headroom wrap codex` calls append a second `[mcp_servers.headroom_memory]` table whenever a valid equivalent entry lacks Headroom's private marker, corrupting `~/.codex/config.toml`; an already-invalid or user-managed conflicting file can also be modified further. Upstream 0.37.0 is still the latest release, and the marker-only implementation remains on upstream main at https://github.com/headroomlabs-ai/headroom/blob/main/headroom/cli/wrap.py. The patch parses before writing, preserves an equivalent entry byte-for-byte, replaces only a stale Headroom-marker-managed block, and refuses to alter invalid or conflicting user-managed TOML. Obsolescence check: inspect upstream `_inject_memory_mcp_config` after every Headroom update and remove this patch once upstream provides equivalent parse-before-write behavior; rerun `nix build .#checks.x86_64-linux.headroom-cli`, whose regression covers repeated equivalent registration, conflicting and invalid files, and concurrent wraps sharing the configured port.
- `packages/headroom-ai.nix` pins Headroom 0.37.0 from its official x86_64 Linux wheel, and `modules/home-manager/headroom.nix` adapts its OpenCode wrapper for declarative Home Manager configuration: targets `homeConfigurations.cachyos-framework13` and the embedded Home Manager user in `nixosConfigurations.razer14`. Original issue: the primary `nixos-unstable` package set does not provide Headroom, while both hosts need its CLI on `PATH`; the upstream launcher also injects Python paths only into its first process even though `headroom wrap opencode` starts the proxy with a fresh `python -m headroom.cli` child, and upstream tries to rewrite OpenCode's config even when Home Manager owns it as an immutable store symlink. The package includes Headroom's core and `[proxy]` runtime dependencies, but not `[all]`; the module supplies `uvx` for upstream's optional Serena integration. The module also applies an intentional local default policy: `headroom wrap codex` and `headroom wrap opencode` inject any missing `--memory` and `--code-graph` flags before target arguments. This retains upstream behavior, including project-local `.headroom/memory.db`, marker-managed memory guidance in `AGENTS.md`, Codex MCP configuration, and a live code-graph watcher rooted at the current directory. For OpenCode specifically, the immutable-config adapter copies the active OpenCode config with mode `0600` beneath `$XDG_RUNTIME_DIR`, points Headroom and the launched OpenCode process at that copy through `OPENCODE_CONFIG`, and removes the session directory when the wrapper exits. This does not restore a resident Headroom service, persistent provider rewrites, or global routing variables; the proxy exists only for a wrapped session. Known upstream limitation: default Serena registration can leave an OpenCode ownership record in `~/.headroom/mcp_installs.json` after the temporary config is removed; this bookkeeping does not route traffic, and the adapter must not blindly clear it because an identical pre-existing record may be legitimate. Remove that limitation only after upstream supports a per-resource ledger path or another concurrency-safe session ownership mechanism. Upstream release: https://github.com/headroomlabs-ai/headroom/releases/tag/v0.37.0. Obsolescence checks: after Headroom reaches the primary Nixpkgs input or upstream publishes a maintained Nix package, replace the wheel derivation; separately remove the immutable-config adapter when upstream supports immutable/declarative OpenCode configuration without rewriting it; remove the default-feature argument injection if upstream gains equivalent persistent preferences or this local policy changes. Verify both Home Manager package lists still contain exactly one `headroom-ai`, run the packaged `headroom --version`, and exercise unadorned wrapped Codex and OpenCode sessions, confirm memory and code graph are enabled, and keep the OpenCode config immutable. Validate with `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath`, `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`, and `nix build .#checks.x86_64-linux.headroom-cli` without activating either host.
- `systemd.services.greetd.restartIfChanged = false` in `modules/system-manager/niri.nix`: target `systemConfigs.cachyos-framework13`. Original issue: System Manager restarts changed service units by default, but greetd owns the active Niri login session, so dependency or generated-unit churn during a switch logs the user out and can interrupt activation before its new state is recorded. This matches the committed Nixpkgs greetd module at https://github.com/NixOS/nixpkgs/blob/e5bdc4a41d4c072fe1e3787eaa0320a384741d44/nixos/modules/services/display-managers/greetd.nix#L164-L165, which disables restart-on-change specifically to avoid killing user sessions, and is supported by https://github.com/numtide/system-manager/pull/438. Changed greetd definitions are installed but take effect only after an intentional restart from SSH/another TTY or a reboot. Obsolescence check: retain this while the repository owns a custom CachyOS greetd unit; remove it only if that unit is replaced by an upstream module carrying equivalent lifecycle protection or System Manager gains display-manager-aware restart deferral. After such a change, switch while Niri is active and verify greetd's `MainPID` and the desktop session survive while activation state advances.
- Explicit `system-graphics.package = pkgs.mesa` and `system-graphics.package32 = pkgs.pkgsi686Linux.mesa` in `flake.nix`: target `systemConfigs.cachyos-framework13`. `nix-system-graphics` revision `ac37f0f3ec0cb15d63a520918433c794d01d9dac` still defaults both options through deprecated `mesa.drivers` aliases in [`system/modules/graphics.nix`](https://github.com/soupglasses/nix-system-graphics/blob/master/system/modules/graphics.nix), producing evaluation warnings. The explicit values use the module's supported package options and resolve to the same Mesa driver packages; Framework-only `enable32Bit = true` is permanent host intent and is not part of this exception. Upstream/default direction: remove only the explicit `package` and `package32` assignments after nix-system-graphics defaults directly to `mesa`, update that flake input specifically, and confirm `nix eval .#systemConfigs.cachyos-framework13.drvPath` is warning-free while `system-graphics.enable32Bit` remains true.
- `nixpkgs-fleet` remains a temporary, target-scoped Fleet exception. Original issue: the locked `nixos-unstable` input does not yet expose the required `fleet-orbit` and `fleet-desktop` packages, while the Framework 13 / CachyOS system-manager configuration needs Fleet 1.59.0 plus reviewed runtime-path and update-runner fixes. The `github:AdrielVelazquez/nixpkgs/fleet_orbit_1-55_1-58` input, locked to commit `c686968c7c8384367c32f9daa2390f34c8739d8a`, supplies exactly those two packages through an overlay passed only to `systemConfigs.cachyos-framework13`; the branch name is historical and its current package version is 1.59.0. Primary `nixpkgs` supplies system-manager itself, Mesa, and every other package. This narrow boundary avoids the `nix.enable` contract break caused when current system-manager inherited the Fleet fork's complete NixOS module set. The configuration contract asserts that system-manager follows primary Nixpkgs, primary Nixpkgs does not expose Fleet, Framework Orbit/Desktop exactly match the fork packages, and Framework graphics exactly match primary Mesa. `nixosConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and the `.#system-manager` CLI app remain entirely on primary Nixpkgs. Upstream status last checked 2026-08-26: Nixpkgs PR #525702 merged at `7e52e1b78e196ca9a562863301379282848275c4` with Fleet 1.55.0, while the locked `nixos-unstable` input still does not expose Fleet. Affected runtime target: `systemConfigs.cachyos-framework13` on Framework 13 / CachyOS Linux x86_64. Validate with `nix eval .#systemConfigs.cachyos-framework13.drvPath`, `nix eval .#checks.x86_64-linux.configuration-contract.drvPath`, and, when practical, `nix build .#systemConfigs.cachyos-framework13`; activate only after separate approval. Obsolescence check: once upstream Fleet reaches `nixos-unstable` with the required 1.59.0/runtime behavior, remove the two-package overlay and `nixpkgs-fleet` input. Advance this branch pin only after reviewing the package diff and rerunning the contract and system build.
- `local.keychron-keyboard.inputGroupFallback = true` in `hosts/cachyos-framework13-system-manager/configuration.nix`: target `systemConfigs.cachyos-framework13`. Keychron Launcher did not connect on the Framework with upstream `pkgs.keychron-udev-rules` alone, whose `hidraw` rule uses `MODE="0660"` with `TAG+="uaccess"` for Keychron vendor `3434`. This local fallback keeps the upstream rule behavior but also assigns Keychron `hidraw` devices to the existing `input` group so the Framework user's supplementary group can grant access when active-session ACLs are not sufficient. Upstream/default direction: remove `inputGroupFallback`, regenerate/switch `systemConfigs.cachyos-framework13`, reload udev, replug the Keychron, and confirm Keychron Launcher connects with only `TAG+="uaccess"`; also check the Keychron `hidraw` node with `udevadm info -q property -n /dev/hidrawX` and `getfacl /dev/hidrawX`.
- `local.bolt` system-manager module in `modules/system-manager/bolt.nix`: target `systemConfigs.cachyos-framework13`. Framework 13 / CachyOS with Apple Studio Display showed a full USB4/Thunderbolt teardown during flicker (`retimer disconnected`, `device disconnected`, USB hub disconnects, and `DP-7` disconnect together). CachyOS does not expose the NixOS `services.hardware.bolt.enable` option through this system-manager config. Native `bolt` is therefore installed only by the explicit `just bootstrap-cachyos-prereqs` host bootstrap; the module verifies `/usr/bin/boltctl`, `/usr/lib/boltd`, and the native static D-Bus unit before activation, and writes `options thunderbolt host_reset=false` through `/etc/modprobe.d/99-thunderbolt-studio-display.conf`. It no longer invokes `pacman` or starts a service from a boot-time setup unit. External reference: https://github.com/arne/nixcfg/blob/a04c9b466ba1e473dbfcaab89a82071875e7a53e/hosts/fox/configuration.nix. Upstream/default direction: remove the native prerequisite and host-reset workaround once the default kernel/firmware stack keeps the Studio Display USB4 DP tunnel stable without `bolt` intervention or `thunderbolt.host_reset=false`; validate by connecting the display after reboot and checking `boltctl`, `/sys/module/thunderbolt/parameters/host_reset`, and `journalctl -b -k` for retimer/device disconnects.
- `local.apple-studio-display-hbr3` system-manager service in `modules/system-manager/apple-studio-display-hbr3.nix`: target `systemConfigs.cachyos-framework13`. Framework 13 / CachyOS with Apple Studio Display repeatedly hotplugs at native 5120x2880@60. Debugfs showed the default link sometimes using HBR2 (`Current: 4 0x14 0`) even though the connector reported and verified HBR3 (`4 0x1e 16`). Writing HBR3 changed the active link to `Current: 4 0x1e 0`, matching the CachyOS forum workaround for avoiding the HBR2+DSC path (https://discuss.cachyos.org/t/fix-apple-studio-display-5k-flicker-black-screen-on-linux-with-amd-macbookpro16-1-by-forcing-displayport-hbr3-and-avoiding-dsc/29495), but later flickers still occurred while already at HBR3. On 2026-06-04, full USB4 resets renumbered the Studio Display connector from DP-7/DP-8 to DP-5/DP-6, so the service was changed to scan all DP-* debugfs link settings by default instead of hardcoding DP-7. This service is therefore a Framework-only stabilizer to keep the link from falling back to HBR2, not a proven full fix for the remaining HPD or USB4 drops. Related Framework report: https://community.frame.work/t/apple-studio-display-flickers-after-firmware-update-to-0-0-3-5/82587. Upstream/default direction: remove this service once the AMD/USB4/Studio Display path no longer falls back to HBR2 or once a kernel/firmware fix makes the debugfs preference unnecessary; validate by confirming the active Studio Display DP connector remains at HBR3 after reboot/hotplug without the service and by running `nix eval .#systemConfigs.cachyos-framework13.drvPath --show-trace`.

Current upstream audit (2026-08-26):

- After resolving the aiKitten exception below, none of the six remaining active exceptions is safely removable. Nixpkgs PR #525702 merged at `7e52e1b78e196ca9a562863301379282848275c4` with Fleet 1.55.0, but the locked `nixos-unstable` input still does not expose Fleet and does not contain the 1.59.0 runtime-path fixes; the target-scoped Fleet 1.59.0 pin remains necessary. Check https://github.com/NixOS/nixpkgs/pull/525702 and https://github.com/AdrielVelazquez/nixpkgs/commit/c686968c7c8384367c32f9daa2390f34c8739d8a.
- The upstream Keychron hidraw rule still uses `MODE="0660", TAG+="uaccess"` without a `GROUP=` fallback. Removing `inputGroupFallback` still requires a local replug and WebHID ACL test.
- system-manager still has no Bolt module, Linux still defaults `thunderbolt.host_reset=true`, and Framework firmware issue #185 remains open: https://github.com/FrameworkComputer/SoftwareFirmwareIssueTracker/issues/185. Framework BIOS 4.02 is available while this host was measured on 3.05, but its notes identify no USB4 or display-link fix and retain PD firmware 0.0.21. A separately approved BIOS update followed by independent hardware retests is an opportunity to reassess both display workarounds, not evidence that either is obsolete.
- The Apple Studio Display USB4/HBR3 failure report remains unresolved: https://community.frame.work/t/apple-studio-display-flickers-after-firmware-update-to-0-0-3-5/82587. No current Linux Studio Display quirk was found that replaces the HBR3 service.

Resolved pinned exceptions:

- 2026-08-19: removed `modules/home-manager/ai-kitten-patch-pkgs-system-warning.nix`. Original issue: aiKitten main commit `12b10b525afc9d7f9895f610a6a82714a87b1680` selected its package with the deprecated `pkgs.system`, so this repository carried a complete copy of the upstream Home Manager module solely to replace that expression with `pkgs.stdenv.hostPlatform.system`; the copy also installed only Codex while upstream installed both Codex and unfree Cursor CLI. Resolution: upstream aiKitten commit `e1434099b8ab197288a0572c9d14f08cda32a984` uses `pkgs.stdenv.hostPlatform.system` and makes the Nix-sandbox integration fixture hermetic by replacing a generated `#!/usr/bin/env bash` fake Kitty executable with one using the active Nix Python interpreter. Follow-up commit `546b44249592b167b58d8830a6bc572649879e00` makes the Home Manager module install only the CLI selected by `settings.provider`, preserving this repository's Codex-only package set while retaining Cursor CLI as aiKitten's default-provider dependency. The `ai-kitten` lock node was updated by itself from `12b10b5` to `546b442`, and the local wrapper now imports `inputs.ai-kitten.homeManagerModules.default` directly with the same opt-in settings. Affected outputs: `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, and `nixosConfigurations.dell-plex`. Validation: before the upstream fixes, Home Manager emitted the `pkgs.system` warning, four Nix-sandbox subprocess tests failed, and the new provider-package contract observed three packages instead of two. Afterward, the warning was absent, all six Kitty subprocess integration tests passed, the provider contract passed for default Cursor, explicit Cursor, Codex, and API-only settings, and upstream `nix flake check --show-trace` passed all 244 tests plus lint, type-check, module, and package checks. Downstream, all four exact outputs evaluated, the old/new parity check found identical `programs.ai-kitten` settings and no package additions or removals, both Home Manager configurations sourced the final upstream package, the configuration contract built, the Framework activation package built without activation, and `just check` passed; no switch command ran.
- 2026-08-18: removed the third-party Niri module and package dependency. Original issue: `sodiboo/niri-flake` main stalled at `9ee3e13b60643448228353097880521658b2fe0e`; Nixpkgs's removal of `libdisplay-info_0_2` then broke it, and its replacement PR #1850 closed unmerged. This repository still depended on that fork's structured settings renderer and `config.lib.niri.actions`, while Nixpkgs offered only Niri 26.04 and the historical policy selected locally built upstream-main snapshots. The epireyn successor was therefore accepted only as a temporary, reviewed module/package boundary with its caches explicitly rejected. Resolution: locked Home Manager contains native `wayland.windowManager.niri` support from merged PR https://github.com/nix-community/home-manager/pull/9685; every action helper and fork-specific settings shape was translated to direct KDL-shaped settings. NixOS session integration now uses the Nixpkgs `programs.niri` module with the package overridden, while system-manager retains the native CachyOS greetd/PAM boundary and installs both user units from that same package. The `niri` input follows canonical `github:niri-wm/niri` main, its Nixpkgs input follows primary Nixpkgs, it introduces no `rust-overlay`, and every enabled Linux consumer selects `inputs.niri.packages.${system}.niri` directly without an overlay, fallback, or third-party cache. The package-provider migration preserved source commit `e9b215fef4b11ad36776553fb8bd45118ef03b03` exactly. Future updates remain targeted through `just update-input niri`: review the lock diff plus upstream `flake.nix`, `Cargo.lock`, locked revision, package version, and binary version, then rerun the contract, both rendered-config builds, exact-package validation, and the no-substitution rebuild; an upstream failure must stop the update rather than fall back to `pkgs.niri`. Affected outputs: `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `darwinConfigurations.PNH46YXX3Y`, `systemConfigs.cachyos-framework13`, and `checks.x86_64-linux.configuration-contract`. Validation: all seven exact evaluations returned derivation paths; the configuration contract and system-manager profile built; normalized old/new Razer and Framework KDL statements matched and both final configs passed the exact package's validator; the official package rebuilt reproducibly with substitutions disabled and reported `niri 26.04 (e9b215f)`; the packaged shutdown target matched CachyOS and the service differed only by using the exact store binary; the active removed-reference search was empty; formatting, diff checks, and `just check` passed; no activation ran.
- 2026-08-18: restored locally built Niri unstable after the initial epireyn migration temporarily selected Nixpkgs Niri 26.04. Original issue: configuration history showed that enabled Niri consumers had used `niri-unstable` snapshots since 2026-03-12, and the active binary remained upstream-main commit `7f26c3ee804fb6ed458ef7fb0e3c794f14e0b3bc`; selecting stable 26.04 therefore reduced source freshness. Resolution: every Linux system, standalone Home Manager, and embedded Home Manager consumer now selects epireyn's direct `niri-unstable` derivation `unstable-2026-08-14-6062844` from upstream commit `606284464d4a99bb35710fee68192bc71085ee7c`, built against primary Nixpkgs without importing the overlay. Disabled `aarch64-darwin` retains primary `pkgs.niri` because the fork publishes Linux packages only. Both Niri cache generations remain rejected and compositor verification used `--rebuild --option substitute false` because the currently activated machine still trusted retired `niri.cachix.org`. Affected outputs: `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `darwinConfigurations.PNH46YXX3Y`, `systemConfigs.cachyos-framework13`, and `checks.x86_64-linux.configuration-contract`. Validation: all seven exact evaluations returned derivation paths, the contract and both rendered configurations built without activation, the exact compositor derivation rebuilt locally with substitution disabled, and no switch command ran.
- 2026-08-18: removed the immutable `sodiboo/niri-flake` PR #1850 pin. Original issue: Nixpkgs removed `libdisplay-info_0_2`, stalled Niri-flake main still required it, and commit `6bb99ff875919f03ea6054026619d999061e1170` was temporarily pinned for the 0.3 migration. Resolution: PR #1850 closed unmerged, so the module source now follows maintained `epireyn/niri-flake` main. The fork is deliberately module-only: all fork overlays and the old Niri Cachix URL/key were removed, Niri and Xwayland Satellite select primary Nixpkgs packages, both NixOS outputs disable the module cache even when Niri itself is disabled, and the configuration contract enforces all four boundaries. Final review found that disabled Dell and Darwin consumers initially inherited the fork module's package default; moving both `pkgs.niri` assignments outside their enable guards and expanding the contract to cover system, standalone, embedded, enabled, and disabled consumers closed that gap. Affected outputs: `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `darwinConfigurations.PNH46YXX3Y`, `systemConfigs.cachyos-framework13`, and `checks.x86_64-linux.configuration-contract`. Validation: all seven exact output evaluations returned derivation paths, both rendered Niri configurations built without activation, Niri 26.04 and Xwayland Satellite 0.8.2 resolved from primary Nixpkgs, both NixOS cache flags were false, the removed-reference search was empty, and formatting plus diff checks passed.
- 2026-08-18: removed the broad coupling that made the entire `system-manager` input follow `nixpkgs-fleet`. Original issue: updating system-manager to `64748b62d6ae74c069234103ce368626bcad8c70` exposed an option-contract mismatch because the Fleet fork revision lacks `nix.enable`, `nix.package`, and `nix.nrBuildUsers`, while current system-manager's Nix adapter defines those options. Resolution: system-manager follows primary `nixpkgs` again, and `parts/system-manager.nix` injects only `fleet-orbit` and `fleet-desktop` from the immutable Fleet fork. Framework Mesa and the system-manager CLI also return to primary Nixpkgs; contract assertions pin those package identities so the exception cannot widen silently. Affected outputs: `systemConfigs.cachyos-framework13`, `checks.x86_64-linux.configuration-contract`, and the `.#system-manager` app. Validation: both exact failing evals returned derivation paths after the targeted one-line lock relationship update.
- 2026-07-27: removed the separate `nixpkgs-master` input and `codexOverlay`. Original issue: the globally selected Fleet fork and normal unstable channels lagged daily Codex releases, so all Codex-consuming configurations sourced it from Nixpkgs master. Resolution: the primary input returned to upstream `nixos-unstable`; channel revision `624af665418d3c65d544145b4d34ad696439570e` provides Codex 0.145.0, identical to Codex 0.145.0 in the formerly locked `nixpkgs-master` revision `8507ef13c07fc501e802cd01409a63c7372b095c`. The normal `pkgs.codex` default is therefore sufficient, and the secondary input, overlay, wiring, and lock node are removed. Affected outputs: `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and `darwinConfigurations.PNH46YXX3Y`. Validation checks the Codex version and exact NixOS, Home Manager, and Darwin derivation outputs.
- 2026-07-27: removed `modules/home-manager/rtk-path-test-dead-code.nix` and restored `home.packages = [ pkgs.rtk ];`. Original issue: RTK 0.43.0 promoted warnings to errors, and Rust 1.97 exposed `FILTERS_TOML` and `TomlFilterRegistry::load` as dead code in the test target, causing Nixpkgs checks to fail. Resolution: upstream `nixos-unstable` revision `624af665418d3c65d544145b4d34ad696439570e` carries RTK 0.44.0 from Nixpkgs commit `7aa383af5dad27b517c2a5485033bd1e5417d113`; RTK 0.44.0 contains the upstream TOML-filter wiring fix, and Nixpkgs PR #546378 merged as `0cb6bdbff808cc1c510817e6b0e63f8d91a1e50c` after validating 2,564 passing tests without the local Rust lint workaround. The current channel snapshot still contains Nixpkgs's temporary packaging workaround, but the direct upstream package builds and that cleanup will arrive automatically with a later channel advance. Affected outputs: all shared Home Manager consumers, including `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and `darwinConfigurations.PNH46YXX3Y`. Validation evaluates each exact derivation and builds the upstream RTK package or a Linux Home Manager activation package without activation.
- 2026-05-27: removed `nixpkgs-kernel-master` from `flake.nix` for target `nixosConfigurations.razer14`. Original issue: the Razer host needed Linux 7.0.10 for the upstream MediaTek Bluetooth resume fix, while the temporary PR pin also supplied OpenRazer 3.12.3 to avoid the Linux 7.0.10 `hid_report_raw_event` build failure. Resolution: `nixos-unstable` provided `linux_7_0` 7.0.10, so `boot.kernelPackages` moved back to the main `nixpkgs` input. The later package-only OpenRazer and `linux_7_0` exceptions are resolved separately below; this entry is retained as the history of removing the broader secondary input. Validation used `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`.
- 2026-07-10: removed the separate `nixpkgs-antigravity` input. Original issue: the prior main Fleet pin lacked `pkgs.antigravity-cli`, requiring a second Nixpkgs package set for `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and their embedded NixOS Home Manager configurations. Resolution: refreshed base `0bb7ec54c8483066ec9d7720e780a5caa71f8612` contains `antigravity-cli` 1.0.12 from merged PR #532344 / commit `3a4588b661380663fb1204cc4b6da1f106ddc6a4`, following initial PR #523246 / commit `f123c32d53beed2732137a65deb285db994c3aec` and binary-name fix PR #524076 / commit `db37039ed1f394f02b6c68605276930b0c73815c`. `modules/home-manager/antigravity-cli.nix` now uses the main `pkgs.antigravity-cli`, and the secondary lock node is gone. Validation: `nix eval --raw .#homeConfigurations.razer14.pkgs.antigravity-cli.version` returned `1.0.12`; `nix eval .#homeConfigurations.razer14.activationPackage.drvPath`, `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath`, `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`, and `nix eval .#nixosConfigurations.dell-plex.config.system.build.toplevel.drvPath` returned derivation paths.
- 2026-07-10: removed the Razer host's `linux_7_0` pin. Original issue: Linux 7.0.10 was selected for the MediaTek Bluetooth resume fix associated with Nixpkgs PR #523596 (`436d0dc2f2c1c949fcc575940ccf5f00b32d9424`), while the host still needed a compatible OpenRazer package. Resolution: the refreshed main package set no longer exposes the EOL `linux_7_0`; `boot.kernelPackages` now uses `pkgs.linuxPackages_latest`, verified at Linux 7.1.3. Affected output: `nixosConfigurations.razer14`. Validation: `nix eval --raw .#nixosConfigurations.razer14.config.boot.kernelPackages.kernel.version` returned `7.1.3`, and `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath` returned a derivation path.
- 2026-07-10: removed the local OpenRazer 3.12.3 source, kernel-module, and daemon overrides from `hosts/razer14/system-overrides.nix`. Original issue: the older pinned OpenRazer failed against Linux 7.0.10, so upstream OpenRazer `v3.12.3` and Nixpkgs commit `99643def59501d1eabb1ca01ef701b66d41908fe` were used locally. Resolution: refreshed Nixpkgs contains OpenRazer 3.12.4 from merged PR #539234 / commit `c4abbfa59497b232098087b63d627d87cb8e1cdf`; the normal `hardware.openrazer` feature settings remain, but package overrides are absent. Affected output: `nixosConfigurations.razer14`. Validation: `nix eval --raw .#nixosConfigurations.razer14.pkgs.openrazer-daemon.version` returned `3.12.4`, and `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath` returned a derivation path.
- 2026-07-10: removed `overlays/asdbctl-path-studio-display-1118.nix` and the handwritten system-manager udev rule. Original issue: Nixpkgs `asdbctl` 1.0.0 covered Apple product `05ac:1114` but not this Studio Display's `05ac:1118`, requiring local `asdbctl` 1.1.0 and rules for `1114`, `1116`, and `1118`. Resolution: Nixpkgs PR #527230 merged as commit `29d945b3ea4c3dc94150c6fffb32db8ec5e8fc79`; refreshed Nixpkgs packages `asdbctl` 1.1.0, and system-manager now sources `${pkgs.asdbctl}/lib/udev/rules.d/20-asd-backlight.rules`. Affected outputs: `homeConfigurations.cachyos-framework13`, `systemConfigs.cachyos-framework13`, and configurations consuming the shared Niri brightness package behavior. Validation: `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.asdbctl.version` returned `1.1.0`; `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` and `nix eval .#systemConfigs.cachyos-framework13.drvPath` returned derivation paths, and the built packaged rule contained product IDs `1114`, `1116`, and `1118`.
- 2026-07-10: removed `home.enableNixpkgsReleaseCheck = false` from `users/adriel-cachyos/default.nix`. Original issue: the mutable Fleet branch and the then-pinned Home Manager reported incompatible release generations, producing a mismatch warning for `homeConfigurations.cachyos-framework13`. Resolution: locked Home Manager commit `cee3f0e7fec85d80c149c7afc29926747f7bd3c2` and refreshed nixpkgs both report release 26.11, so the upstream release check remains enabled and no suppression is needed. Validation: `nix eval --raw .#homeConfigurations.cachyos-framework13.config.home.version.release` and `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.lib.trivial.release` both returned `26.11`; `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` returned a derivation path without the release mismatch warning.
- 2026-07-10: removed the local Headroom 0.31.0 package, Home Manager proxy service, OpenCode routing overrides, and obsolete Codex transport patch history from active configuration. Original issue: Headroom was pinned outside Nixpkgs to compress AI-agent context, and earlier releases required a local Codex HTTP fallback because WebSocket Responses traffic bypassed compression. Upstream 0.31.0 removed the transport-patch need, but Framework measurements showed only a 4.6 percent whole-request proxy reduction while the service used about 1 GB RSS, roughly 82 percent of one CPU core, and repeatedly skipped Kompress work under ONNX saturation. Resolution: OpenCode now uses the LLM Platform plugin directly; all `local.headroom` options, service declarations, package derivations, routing headers, and Headroom environment variables are removed. Independent RTK command filtering remains enabled. Affected outputs: `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, and `nixosConfigurations.dell-plex`. Validation: all four exact output evaluations returned derivation paths, and tracked Nix configuration contains no `local.headroom`, `HEADROOM_`, `x-headroom-`, `headroom-ai`, or `headroom-proxy` references.
- 2026-07-10: removed `lib.mkForce` from `services.userborn.enable` in `hosts/cachyos-framework13-system-manager/configuration.nix`. Original issue: system-manager enabled userborn by default and a normal assignment could not override it, tracked in numtide/system-manager issue #350 (https://github.com/numtide/system-manager/issues/350). Resolution: the issue is closed and the current locked system-manager module accepts the ordinary `services.userborn.enable = false;`, so `lib` and the force workaround are unnecessary. Affected output: `systemConfigs.cachyos-framework13`. Validation: `nix eval .#systemConfigs.cachyos-framework13.drvPath` returned a derivation path, and `git grep -n 'services.userborn.enable = lib.mkForce false' -- '*.nix'` returned no matches.
- 2026-07-10: removed the direct `${inputs.nixpkgs}/nixos/modules/security/dhparams.nix` import from `parts/system-manager.nix`. Original issue: the system-manager integration previously depended on the NixOS dhparams module and imported it explicitly as a compatibility workaround for `systemConfigs.cachyos-framework13`. Resolution: refreshed Nixpkgs removed `security.dhparams` in favor of RFC 7919 parameters, so the path no longer exists and the stale import itself blocked evaluation; current system-manager evaluates without it. Validation: `nix eval .#systemConfigs.cachyos-framework13.drvPath` returned a derivation path, and `git grep -n 'nixos/modules/security/dhparams.nix' -- '*.nix'` returned no matches.
