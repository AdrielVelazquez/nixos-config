Pinned exceptions to revisit:

- `nixpkgs-kernel-master` in `flake.nix`: target `nixosConfigurations.razer14`. Points at Nixpkgs PR #523596 commit `436d0dc2f2c1c949fcc575940ccf5f00b32d9424` to keep Linux 7.0.10 for the upstream MediaTek Bluetooth fix while also picking up OpenRazer 3.12.3, which fixes the Linux 7.0.10 `hid_report_raw_event` build failure. Upstream: https://github.com/NixOS/nixpkgs/pull/523596. Checked 2026-05-25: PR #523596 is closed without merge, current `nixos-unstable` has `linux_7_0` 7.0.10 but still has `openrazer-daemon` 3.12.2, while the local `razer14` config evaluates to kernel 7.0.10 via this pin. Next check: remove this PR pin once the main `nixos-unstable` input has Linux 7.0.10 or newer and OpenRazer 3.12.3 or an equivalent OpenRazer Linux 7.0.10 build fix.
- `nixpkgs-antigravity` in `flake.nix`: target `homeConfigurations.adriel`, plus embedded Home Manager in `nixosConfigurations.razer14` and `nixosConfigurations.dell`. Follows nixpkgs `master` for `antigravity-cli` while the package is not available in the main pinned `nixpkgs` input. Upstream: merged Nixpkgs PR #523246 / commit `f123c32d53beed2732137a65deb285db994c3aec` (`antigravity-cli` 1.0.2), followed by merged PR #524076 / commit `db37039ed1f394f02b6c68605276930b0c73815c` (`antigravity-cli: fix binary name`). Checked 2026-05-25: `master` and the current `nixpkgs-antigravity` lock rev `48713cdf7be158a30721e7971d12da155dad2801` expose `meta.mainProgram = "agy"` and install `$out/bin/agy`; current `nixos-unstable` does not have the package yet. Next check: remove this input once `nixos-unstable` has `pkgs.antigravity-cli` with the `agy` binary.
