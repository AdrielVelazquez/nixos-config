Pinned exceptions to revisit:

- `nixpkgs-fleet` remains a temporary, target-scoped Fleet exception. Original issue: upstream `nixos-unstable` does not yet contain `fleet-orbit` or `fleet-desktop` (verified at channel revision `624af665418d3c65d544145b4d34ad696439570e` on 2026-07-27), while the Framework 13 / CachyOS system-manager configuration needs both packages. The primary `nixpkgs` input now follows `github:NixOS/nixpkgs/nixos-unstable`; only the `system-manager` input follows the immutable `github:AdrielVelazquez/nixpkgs/15b70b1d5954a2573a7d6a0228eb2c5de4733db8` pin from `fleet-orbit-improvements`, which provides Fleet 1.58.0 and the reviewed runtime, module, test, update, and documentation improvements. The globally imported NixOS service bundle no longer imports its unused Orbit adapter, so Razer, Dell, standalone Home Manager, and Darwin remain entirely on the primary package set. Upstream status checked 2026-07-27: Nixpkgs PR #525702 remains open and unmerged at rebased head `d2c59b6d82b9a81e12892b1d3fc6356fa383d7d1`; it still packages Fleet 1.55.0, has one approval label, and its Linux package/test and combined status checks pass. The improvements branch remains at `15b70b1d5954a2573a7d6a0228eb2c5de4733db8`. Affected runtime target: `systemConfigs.cachyos-framework13` on framework13 / CachyOS Linux x86_64, plus the pinned `.#system-manager` CLI app used to activate it; `systemConfigs.default` is removed. Validate with `nix eval .#systemConfigs.cachyos-framework13.drvPath` and `nix build .#systemConfigs.cachyos-framework13`; activate only after separate approval. Rollback only the isolated `nixpkgs-fleet` input to `ee772785e7d7d124b81c80dbfaa41ba64244bf86`, update that input specifically, and restore the prior Framework Orbit adapter; do not move the primary input off upstream. Obsolescence check: inspect https://github.com/NixOS/nixpkgs/pull/525702 and wait for the merged Fleet implementation to reach `nixos-unstable`. Confirm that upstream covers the required 1.58.0/runtime behavior before switching system-manager back to primary `nixpkgs` and removing `nixpkgs-fleet`. Do not blindly replace the improvements pin with a rebased PR head or force-push/repurpose `fleet_intial_package`.
- `local.keychron-keyboard.inputGroupFallback = true` in `hosts/cachyos-framework13-system-manager/configuration.nix`: target `systemConfigs.cachyos-framework13`. Keychron Launcher did not connect on the Framework with upstream `pkgs.keychron-udev-rules` alone, whose `hidraw` rule uses `MODE="0660"` with `TAG+="uaccess"` for Keychron vendor `3434`. This local fallback keeps the upstream rule behavior but also assigns Keychron `hidraw` devices to the existing `input` group so the Framework user's supplementary group can grant access when active-session ACLs are not sufficient. Upstream/default direction: remove `inputGroupFallback`, regenerate/switch `systemConfigs.cachyos-framework13`, reload udev, replug the Keychron, and confirm Keychron Launcher connects with only `TAG+="uaccess"`; also check the Keychron `hidraw` node with `udevadm info -q property -n /dev/hidrawX` and `getfacl /dev/hidrawX`.
- `local.bolt` system-manager module in `modules/system-manager/bolt.nix`: target `systemConfigs.cachyos-framework13`. Framework 13 / CachyOS with Apple Studio Display showed a full USB4/Thunderbolt teardown during flicker (`retimer disconnected`, `device disconnected`, USB hub disconnects, and `DP-7` disconnect together). CachyOS does not expose the NixOS `services.hardware.bolt.enable` option through this system-manager config. Native `bolt` is therefore installed only by the explicit `just bootstrap-cachyos-prereqs` host bootstrap; the module verifies `/usr/bin/boltctl`, `/usr/lib/boltd`, and the native static D-Bus unit before activation, and writes `options thunderbolt host_reset=false` through `/etc/modprobe.d/99-thunderbolt-studio-display.conf`. It no longer invokes `pacman` or starts a service from a boot-time setup unit. External reference: https://github.com/arne/nixcfg/blob/a04c9b466ba1e473dbfcaab89a82071875e7a53e/hosts/fox/configuration.nix. Upstream/default direction: remove the native prerequisite and host-reset workaround once the default kernel/firmware stack keeps the Studio Display USB4 DP tunnel stable without `bolt` intervention or `thunderbolt.host_reset=false`; validate by connecting the display after reboot and checking `boltctl`, `/sys/module/thunderbolt/parameters/host_reset`, and `journalctl -b -k` for retimer/device disconnects.
- `local.apple-studio-display-hbr3` system-manager service in `modules/system-manager/apple-studio-display-hbr3.nix`: target `systemConfigs.cachyos-framework13`. Framework 13 / CachyOS with Apple Studio Display repeatedly hotplugs at native 5120x2880@60. Debugfs showed the default link sometimes using HBR2 (`Current: 4 0x14 0`) even though the connector reported and verified HBR3 (`4 0x1e 16`). Writing HBR3 changed the active link to `Current: 4 0x1e 0`, matching the CachyOS forum workaround for avoiding the HBR2+DSC path (https://discuss.cachyos.org/t/fix-apple-studio-display-5k-flicker-black-screen-on-linux-with-amd-macbookpro16-1-by-forcing-displayport-hbr3-and-avoiding-dsc/29495), but later flickers still occurred while already at HBR3. On 2026-06-04, full USB4 resets renumbered the Studio Display connector from DP-7/DP-8 to DP-5/DP-6, so the service was changed to scan all DP-* debugfs link settings by default instead of hardcoding DP-7. This service is therefore a Framework-only stabilizer to keep the link from falling back to HBR2, not a proven full fix for the remaining HPD or USB4 drops. Related Framework report: https://community.frame.work/t/apple-studio-display-flickers-after-firmware-update-to-0-0-3-5/82587. Upstream/default direction: remove this service once the AMD/USB4/Studio Display path no longer falls back to HBR2 or once a kernel/firmware fix makes the debugfs preference unnecessary; validate by confirming the active Studio Display DP connector remains at HBR3 after reboot/hotplug without the service and by running `nix eval .#systemConfigs.cachyos-framework13.drvPath --show-trace`.
- `ai-kitten-patch-pkgs-system-warning.nix` in `modules/home-manager`: target `homeConfigurations.cachyos-framework13`. Temporarily copies the upstream ai-kitten Home Manager module and replaces `self.packages.${pkgs.system}.ai-kitten` with `inputs.ai-kitten.packages.${pkgs.stdenv.hostPlatform.system}.ai-kitten` to avoid the deprecated `pkgs.system` evaluation warning. Upstream: private `git@github.com:AdrielVelazquez/aiKitten.git` main commit `12b10b525afc9d7f9895f610a6a82714a87b1680` still has the deprecated access in `flake.nix`. Checked 2026-07-10 with `rtk git ls-remote git@github.com:AdrielVelazquez/aiKitten.git refs/heads/main`: upstream `refs/heads/main` remains `12b10b525afc9d7f9895f610a6a82714a87b1680`. Next check: rerun `rtk git ls-remote git@github.com:AdrielVelazquez/aiKitten.git refs/heads/main`, update the `ai-kitten` flake input if it changed, and remove the local patch once upstream uses `pkgs.stdenv.hostPlatform.system`; validate with `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath --show-trace`.

Current upstream audit (2026-07-28):

- None of the five active exceptions is safely removable. Nixpkgs PR #525702 remains open at `d2c59b6d82b9a81e12892b1d3fc6356fa383d7d1`, still packages Fleet 1.55.0, and has not added Fleet packages to `master` or `nixos-unstable`; the target-scoped Fleet 1.58.0 pin remains necessary. Check https://github.com/NixOS/nixpkgs/pull/525702 and https://github.com/AdrielVelazquez/nixpkgs/commit/15b70b1d5954a2573a7d6a0228eb2c5de4733db8.
- The upstream Keychron hidraw rule still uses `MODE="0660", TAG+="uaccess"` without a `GROUP=` fallback. Removing `inputGroupFallback` still requires a local replug and WebHID ACL test.
- system-manager still has no Bolt module, Linux still defaults `thunderbolt.host_reset=true`, and Framework firmware issue #185 remains open: https://github.com/FrameworkComputer/SoftwareFirmwareIssueTracker/issues/185. Framework BIOS 4.02 is available while this host was measured on 3.05, but its notes identify no USB4 or display-link fix and retain PD firmware 0.0.21. A separately approved BIOS update followed by independent hardware retests is an opportunity to reassess both display workarounds, not evidence that either is obsolete.
- The Apple Studio Display USB4/HBR3 failure report remains unresolved: https://community.frame.work/t/apple-studio-display-flickers-after-firmware-update-to-0-0-3-5/82587. No current Linux Studio Display quirk was found that replaces the HBR3 service.
- Private aiKitten `refs/heads/main` remains exactly `12b10b525afc9d7f9895f610a6a82714a87b1680`, verified over read-only SSH. The GitHub connector could not inspect the private file contents, so only the unchanged ref was freshly confirmed.

Resolved pinned exceptions:

- 2026-07-27: removed the separate `nixpkgs-master` input and `codexOverlay`. Original issue: the globally selected Fleet fork and normal unstable channels lagged daily Codex releases, so all Codex-consuming configurations sourced it from Nixpkgs master. Resolution: the primary input returned to upstream `nixos-unstable`; channel revision `624af665418d3c65d544145b4d34ad696439570e` provides Codex 0.145.0, identical to Codex 0.145.0 in the formerly locked `nixpkgs-master` revision `8507ef13c07fc501e802cd01409a63c7372b095c`. The normal `pkgs.codex` default is therefore sufficient, and the secondary input, overlay, wiring, and lock node are removed. Affected outputs: `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and `darwinConfigurations.PNH46YXX3Y`. Validation checks the Codex version and exact NixOS, Home Manager, and Darwin derivation outputs.
- 2026-07-27: removed `modules/home-manager/rtk-path-test-dead-code.nix` and restored `home.packages = [ pkgs.rtk ];`. Original issue: RTK 0.43.0 promoted warnings to errors, and Rust 1.97 exposed `FILTERS_TOML` and `TomlFilterRegistry::load` as dead code in the test target, causing Nixpkgs checks to fail. Resolution: upstream `nixos-unstable` revision `624af665418d3c65d544145b4d34ad696439570e` carries RTK 0.44.0 from Nixpkgs commit `7aa383af5dad27b517c2a5485033bd1e5417d113`; RTK 0.44.0 contains the upstream TOML-filter wiring fix, and Nixpkgs PR #546378 merged as `0cb6bdbff808cc1c510817e6b0e63f8d91a1e50c` after validating 2,564 passing tests without the local Rust lint workaround. The current channel snapshot still contains Nixpkgs's temporary packaging workaround, but the direct upstream package builds and that cleanup will arrive automatically with a later channel advance. Affected outputs: all shared Home Manager consumers, including `nixosConfigurations.razer14`, `nixosConfigurations.dell-plex`, `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and `darwinConfigurations.PNH46YXX3Y`. Validation evaluates each exact derivation and builds the upstream RTK package or a Linux Home Manager activation package without activation.
- 2026-05-27: removed `nixpkgs-kernel-master` from `flake.nix` for target `nixosConfigurations.razer14`. Original issue: the Razer host needed Linux 7.0.10 for the upstream MediaTek Bluetooth resume fix, while the temporary PR pin also supplied OpenRazer 3.12.3 to avoid the Linux 7.0.10 `hid_report_raw_event` build failure. Resolution: `nixos-unstable` provided `linux_7_0` 7.0.10, so `boot.kernelPackages` moved back to the main `nixpkgs` input. The later package-only OpenRazer and `linux_7_0` exceptions are resolved separately below; this entry is retained as the history of removing the broader secondary input. Validation used `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`.
- 2026-07-10: removed the separate `nixpkgs-antigravity` input. Original issue: the prior main Fleet pin lacked `pkgs.antigravity-cli`, requiring a second Nixpkgs package set for `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, and their embedded NixOS Home Manager configurations. Resolution: refreshed base `0bb7ec54c8483066ec9d7720e780a5caa71f8612` contains `antigravity-cli` 1.0.12 from merged PR #532344 / commit `3a4588b661380663fb1204cc4b6da1f106ddc6a4`, following initial PR #523246 / commit `f123c32d53beed2732137a65deb285db994c3aec` and binary-name fix PR #524076 / commit `db37039ed1f394f02b6c68605276930b0c73815c`. `modules/home-manager/antigravity-cli.nix` now uses the main `pkgs.antigravity-cli`, and the secondary lock node is gone. Validation: `nix eval --raw .#homeConfigurations.razer14.pkgs.antigravity-cli.version` returned `1.0.12`; `nix eval .#homeConfigurations.razer14.activationPackage.drvPath`, `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath`, `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`, and `nix eval .#nixosConfigurations.dell-plex.config.system.build.toplevel.drvPath` returned derivation paths.
- 2026-07-10: removed the Razer host's `linux_7_0` pin. Original issue: Linux 7.0.10 was selected for the MediaTek Bluetooth resume fix associated with Nixpkgs PR #523596 (`436d0dc2f2c1c949fcc575940ccf5f00b32d9424`), while the host still needed a compatible OpenRazer package. Resolution: the refreshed main package set no longer exposes the EOL `linux_7_0`; `boot.kernelPackages` now uses `pkgs.linuxPackages_latest`, verified at Linux 7.1.3. Affected output: `nixosConfigurations.razer14`. Validation: `nix eval --raw .#nixosConfigurations.razer14.config.boot.kernelPackages.kernel.version` returned `7.1.3`, and `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath` returned a derivation path.
- 2026-07-10: removed the local OpenRazer 3.12.3 source, kernel-module, and daemon overrides from `hosts/razer14/system-overrides.nix`. Original issue: the older pinned OpenRazer failed against Linux 7.0.10, so upstream OpenRazer `v3.12.3` and Nixpkgs commit `99643def59501d1eabb1ca01ef701b66d41908fe` were used locally. Resolution: refreshed Nixpkgs contains OpenRazer 3.12.4 from merged PR #539234 / commit `c4abbfa59497b232098087b63d627d87cb8e1cdf`; the normal `hardware.openrazer` feature settings remain, but package overrides are absent. Affected output: `nixosConfigurations.razer14`. Validation: `nix eval --raw .#nixosConfigurations.razer14.pkgs.openrazer-daemon.version` returned `3.12.4`, and `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath` returned a derivation path.
- 2026-07-10: removed `overlays/asdbctl-path-studio-display-1118.nix` and the handwritten system-manager udev rule. Original issue: Nixpkgs `asdbctl` 1.0.0 covered Apple product `05ac:1114` but not this Studio Display's `05ac:1118`, requiring local `asdbctl` 1.1.0 and rules for `1114`, `1116`, and `1118`. Resolution: Nixpkgs PR #527230 merged as commit `29d945b3ea4c3dc94150c6fffb32db8ec5e8fc79`; refreshed Nixpkgs packages `asdbctl` 1.1.0, and system-manager now sources `${pkgs.asdbctl}/lib/udev/rules.d/20-asd-backlight.rules`. Affected outputs: `homeConfigurations.cachyos-framework13`, `systemConfigs.cachyos-framework13`, and configurations consuming the shared Niri brightness package behavior. Validation: `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.asdbctl.version` returned `1.1.0`; `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` and `nix eval .#systemConfigs.cachyos-framework13.drvPath` returned derivation paths, and the built packaged rule contained product IDs `1114`, `1116`, and `1118`.
- 2026-07-10: removed `home.enableNixpkgsReleaseCheck = false` from `users/adriel-cachyos/default.nix`. Original issue: the mutable Fleet branch and the then-pinned Home Manager reported incompatible release generations, producing a mismatch warning for `homeConfigurations.cachyos-framework13`. Resolution: locked Home Manager commit `cee3f0e7fec85d80c149c7afc29926747f7bd3c2` and refreshed nixpkgs both report release 26.11, so the upstream release check remains enabled and no suppression is needed. Validation: `nix eval --raw .#homeConfigurations.cachyos-framework13.config.home.version.release` and `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.lib.trivial.release` both returned `26.11`; `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` returned a derivation path without the release mismatch warning.
- 2026-07-10: removed the local Headroom 0.31.0 package, Home Manager proxy service, OpenCode routing overrides, and obsolete Codex transport patch history from active configuration. Original issue: Headroom was pinned outside Nixpkgs to compress AI-agent context, and earlier releases required a local Codex HTTP fallback because WebSocket Responses traffic bypassed compression. Upstream 0.31.0 removed the transport-patch need, but Framework measurements showed only a 4.6 percent whole-request proxy reduction while the service used about 1 GB RSS, roughly 82 percent of one CPU core, and repeatedly skipped Kompress work under ONNX saturation. Resolution: OpenCode now uses the LLM Platform plugin directly; all `local.headroom` options, service declarations, package derivations, routing headers, and Headroom environment variables are removed. Independent RTK command filtering remains enabled. Affected outputs: `homeConfigurations.razer14`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, and `nixosConfigurations.dell-plex`. Validation: all four exact output evaluations returned derivation paths, and tracked Nix configuration contains no `local.headroom`, `HEADROOM_`, `x-headroom-`, `headroom-ai`, or `headroom-proxy` references.
- 2026-07-10: removed `lib.mkForce` from `services.userborn.enable` in `hosts/cachyos-framework13-system-manager/configuration.nix`. Original issue: system-manager enabled userborn by default and a normal assignment could not override it, tracked in numtide/system-manager issue #350 (https://github.com/numtide/system-manager/issues/350). Resolution: the issue is closed and the current locked system-manager module accepts the ordinary `services.userborn.enable = false;`, so `lib` and the force workaround are unnecessary. Affected output: `systemConfigs.cachyos-framework13`. Validation: `nix eval .#systemConfigs.cachyos-framework13.drvPath` returned a derivation path, and `git grep -n 'services.userborn.enable = lib.mkForce false' -- '*.nix'` returned no matches.
- 2026-07-10: removed the direct `${inputs.nixpkgs}/nixos/modules/security/dhparams.nix` import from `parts/system-manager.nix`. Original issue: the system-manager integration previously depended on the NixOS dhparams module and imported it explicitly as a compatibility workaround for `systemConfigs.cachyos-framework13`. Resolution: refreshed Nixpkgs removed `security.dhparams` in favor of RFC 7919 parameters, so the path no longer exists and the stale import itself blocked evaluation; current system-manager evaluates without it. Validation: `nix eval .#systemConfigs.cachyos-framework13.drvPath` returned a derivation path, and `git grep -n 'nixos/modules/security/dhparams.nix' -- '*.nix'` returned no matches.
