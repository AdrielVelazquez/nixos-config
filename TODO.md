Pinned exceptions to revisit:

- `openrazer` 3.12.3 override in `hosts/razer14/system-overrides.nix`: target `nixosConfigurations.razer14`. Pins only the OpenRazer kernel module and daemon to upstream release `v3.12.3` while keeping `boot.kernelPackages` on the main `nixpkgs` input's `linux_7_0`. Original issue: `nixos-unstable` has Linux 7.0.10, but its OpenRazer 3.12.2 fails for this host/kernel; 3.12.3 is the known-good version from the previous PR pin. Upstream: OpenRazer `v3.12.3` and Nixpkgs master commit `99643def59501d1eabb1ca01ef701b66d41908fe`. Checked 2026-05-27: current `nixos-unstable` rev `64c08a7ca051951c8eae34e3e3cb1e202fe36786` still has OpenRazer 3.12.2. Next check: remove this override once `nixos-unstable` has OpenRazer 3.12.3 or an equivalent Linux 7.0.10 build fix; validate with `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`.
- `nixpkgs-antigravity` in `flake.nix`: target `homeConfigurations.adriel` and `homeConfigurations.cachyos-framework13`, plus embedded Home Manager in `nixosConfigurations.razer14` and `nixosConfigurations.dell`. Follows `NixOS/nixpkgs` `master` at lock rev `5e75619fdabe0776585176d95429234a53d2239b` for `antigravity-cli` 1.0.3 while the package is not available in the main pinned `nixpkgs` input. Upstream: merged Nixpkgs PR #523246 / commit `f123c32d53beed2732137a65deb285db994c3aec` (`antigravity-cli` 1.0.2), followed by merged PR #524076 / commit `db37039ed1f394f02b6c68605276930b0c73815c` (`antigravity-cli: fix binary name`), and current `NixOS/nixpkgs` master commit `5e75619fdabe0776585176d95429234a53d2239b` (`antigravity-cli` 1.0.3). Checked 2026-05-27: `NixOS/nixpkgs` master exposes `version = "1.0.3"` and `meta.mainProgram = "agy"`; current main `nixpkgs` input rev `64c08a7ca051951c8eae34e3e3cb1e202fe36786` still does not expose `pkgs.antigravity-cli`. Next check: remove this input once `nixos-unstable` has `pkgs.antigravity-cli` with the needed version and binary.
- `ai-kitten-patch-pkgs-system-warning.nix` in `modules/home-manager`: target `homeConfigurations.cachyos-framework13`. Temporarily copies the upstream ai-kitten Home Manager module and replaces `self.packages.${pkgs.system}.ai-kitten` with `inputs.ai-kitten.packages.${pkgs.stdenv.hostPlatform.system}.ai-kitten` to avoid the nixpkgs 26.11pre `pkgs.system` evaluation warning. Upstream: private `git@github.com:AdrielVelazquez/aiKitten.git` main commit `12b10b525afc9d7f9895f610a6a82714a87b1680` still has the deprecated access in `flake.nix`. Checked 2026-05-29: upstream `refs/heads/main` is still `12b10b525afc9d7f9895f610a6a82714a87b1680`. Next check: run `git ls-remote git@github.com:AdrielVelazquez/aiKitten.git refs/heads/main`, update the `ai-kitten` flake input if it changed, and remove the local patch once upstream uses `pkgs.stdenv.hostPlatform.system`; validate with `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath --show-trace`.
- `home.enableNixpkgsReleaseCheck = false` in `users/adriel-cachyos/default.nix`: target `homeConfigurations.cachyos-framework13`. Suppresses the Home Manager release mismatch warning while this config follows the main `nixpkgs` input at `AdrielVelazquez/nixpkgs` `fleet_intial_package`, which currently reports `26.11pre-git`, and Home Manager master is still `26.05`. Upstream: Home Manager master `refs/heads/master` is `61e2c9659324181e0f0ed911958c536333b1d4f6`, matching the current lock, so there is no newer upstream Home Manager commit to consume. Checked 2026-05-29. Next check: remove this option once the main `nixpkgs` input returns to a release-compatible branch or Home Manager reports the matching release; validate with `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath --show-trace`.
- `asdbctl` v1.1.0 override in `overlays/asdbctl-path-studio-display-1118.nix` and matching system-manager udev rule in `modules/system-manager/apple-studio-display-brightness.nix`: target `homeConfigurations.cachyos-framework13`, `systemConfigs.cachyos-framework`, and any Niri configs that enable Apple Studio Display brightness support. Current pinned `nixpkgs` and Nixpkgs master still package `asdbctl` 1.0.0, whose udev rule only covers Apple product ID `05ac:1114`; this Studio Display exposes `05ac:1118`, which upstream `asdbctl` v1.1.0 covers alongside `1114` and `1116`. Upstream: `juliuszint/asdbctl` release `v1.1.0`; Nixpkgs has only the merged init PR #392023 and no observed 1.1.0 update PR. Checked 2026-06-02. Next check: remove the override and the local system-manager rule once `nixos-unstable` has `asdbctl` >= 1.1.0 with the expanded udev rules; validate with `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` and the matching `systemConfigs.cachyos-framework` eval.

Resolved pinned exceptions:

- 2026-05-27: removed `nixpkgs-kernel-master` from `flake.nix` for target `nixosConfigurations.razer14`. Original issue: the Razer host needed Linux 7.0.10 for the upstream MediaTek Bluetooth resume fix, while the temporary PR pin also supplied OpenRazer 3.12.3 to avoid the Linux 7.0.10 `hid_report_raw_event` build failure. Resolution: `nixos-unstable` now provides `linux_7_0` 7.0.10, so `boot.kernelPackages` was moved back to the main `nixpkgs` input. OpenRazer remains tracked above as a smaller package-only exception.
