Pinned exceptions to revisit:

- Separate `nixpkgs-master` flake input (`github:NixOS/nixpkgs`, tracking master) sources `codex` for all configurations that consume `pkgs.codex`: `nixosConfigurations.razer14`, `nixosConfigurations.dell`, `homeConfigurations.adriel`, `homeConfigurations.cachyos-framework13`, and `darwinConfigurations.PNH46YXX3Y` (via `modules/home-manager/codex-cli.nix`). Reason: the main pinned `nixpkgs` fork (Fleet branch at `ee772785e7d7d124b81c80dbfaa41ba64244bf86`) and upstream `nixos-unstable`/`nixpkgs-unstable` carry codex `0.142.5`, while nixpkgs `master` carries `0.144.1`; codex releases roughly daily and channels lag master. `codexOverlay` in `parts/lib.nix` replaces `pkgs.codex` with `inputs.nixpkgs-master.legacyPackages.<system>.codex`, wired through `mkHomeManagerConfig` (NixOS + Darwin) and `parts/home-manager.nix` (standalone home configs). `just update` advances this input so codex tracks upstream automatically. Obsolescence check: once the main `nixpkgs` input carries a codex at least as new as what you want, run `nix eval --raw .#homeConfigurations.adriel.pkgs.codex.version` against the main input (temporarily drop the overlay) to confirm, then remove the `nixpkgs-master` input, `codexOverlay`, and its wiring, and drop the lock node. Validate removal with `nix eval --raw .#nixosConfigurations.razer14.pkgs.codex.version`, `nix eval --raw .#homeConfigurations.adriel.pkgs.codex.version`, `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.codex.version`, plus `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`, `nix eval .#nixosConfigurations.dell.config.system.build.toplevel.drvPath`, `nix eval .#homeConfigurations.adriel.activationPackage.drvPath`, `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath`, and `nix eval .#darwinConfigurations.PNH46YXX3Y.config.system.build.toplevel.drvPath`.
- Fleet remains a fork exception in the main `nixpkgs` input for all exported configurations: `nixosConfigurations.razer14`, `nixosConfigurations.dell`, `homeConfigurations.adriel`, `homeConfigurations.cachyos-framework13`, `systemConfigs.cachyos-framework`, and `darwinConfigurations.PNH46YXX3Y`. Branch `fleet-refresh-2026-07-10` in `AdrielVelazquez/nixpkgs` starts from upstream `nixos-unstable` commit `0bb7ec54c8483066ec9d7720e780a5caa71f8612`, replays original Fleet package commit `6a42e37e705f1017c41968669b3826e0ceb5584a` and module commit `0d21d368fce5d893714d4b097eeb705a3291eafe` in that order, and is consumed immutably at `ee772785e7d7d124b81c80dbfaa41ba64244bf86`. The corresponding upstream work is open Nixpkgs PR #525702 (`fleet-{desktop,orbit}: init at 1.55.0`, https://github.com/NixOS/nixpkgs/pull/525702). Obsolescence check: run `gh pr view 525702 --repo NixOS/nixpkgs --json state,mergeCommit,url`, then verify the repository's normal `nixos-unstable` input contains `fleet-orbit`, `fleet-desktop`, and the Orbit NixOS module/test; remove the fork only after `NIXPKGS_ALLOW_UNFREE=1 nix-build -A fleet-orbit -A fleet-desktop -A nixosTests.orbit --no-out-link` succeeds from that upstream revision and these exact evaluations return derivation paths: `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`, `nix eval .#nixosConfigurations.dell.config.system.build.toplevel.drvPath`, `nix eval .#homeConfigurations.adriel.activationPackage.drvPath`, `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath`, `nix eval .#systemConfigs.cachyos-framework.drvPath`, and `nix eval .#darwinConfigurations.PNH46YXX3Y.config.system.build.toplevel.drvPath`. If upstream is still missing Fleet, refresh without rewriting history: create a new branch from the latest `NixOS/nixpkgs:nixos-unstable`, cherry-pick the two original commits in package-then-module order, build the three deliverables, push the new branch without `--force`, pin the resulting literal commit hash in `flake.nix`, and update only the `nixpkgs` lock input. Never force-push or repurpose `fleet_intial_package` or this dated refresh branch.
- `local.keychron-keyboard.inputGroupFallback = true` in `hosts/cachyos-framework13-system-manager/configuration.nix`: target `systemConfigs.cachyos-framework`. Keychron Launcher did not connect on the Framework with upstream `pkgs.keychron-udev-rules` alone, whose `hidraw` rule uses `MODE="0660"` with `TAG+="uaccess"` for Keychron vendor `3434`. This local fallback keeps the upstream rule behavior but also assigns Keychron `hidraw` devices to the existing `input` group so the Framework user's supplementary group can grant access when active-session ACLs are not sufficient. Upstream/default direction: remove `inputGroupFallback`, regenerate/switch `systemConfigs.cachyos-framework`, reload udev, replug the Keychron, and confirm Keychron Launcher connects with only `TAG+="uaccess"`; also check the Keychron `hidraw` node with `udevadm info -q property -n /dev/hidrawX` and `getfacl /dev/hidrawX`.
- `local.bolt` system-manager service in `modules/system-manager/bolt.nix`: target `systemConfigs.cachyos-framework`. Framework 13 / CachyOS with Apple Studio Display showed a full USB4/Thunderbolt teardown during flicker (`retimer disconnected`, `device disconnected`, USB hub disconnects, and `DP-7` disconnect together). CachyOS does not expose the NixOS `services.hardware.bolt.enable` option through this system-manager config, so this module installs native `bolt` with pacman, starts the static D-Bus activated `bolt.service`, and writes `options thunderbolt host_reset=false` through `/etc/modprobe.d/99-thunderbolt-studio-display.conf`. External reference: https://github.com/arne/nixcfg/blob/a04c9b466ba1e473dbfcaab89a82071875e7a53e/hosts/fox/configuration.nix. Upstream/default direction: remove the native setup and host-reset workaround once the default kernel/firmware stack keeps the Studio Display USB4 DP tunnel stable without `bolt` intervention or `thunderbolt.host_reset=false`; validate by connecting the display after reboot and checking `boltctl`, `/sys/module/thunderbolt/parameters/host_reset`, and `journalctl -b -k` for retimer/device disconnects.
- `local.apple-studio-display-hbr3` system-manager service in `modules/system-manager/apple-studio-display-hbr3.nix`: target `systemConfigs.cachyos-framework`. Framework 13 / CachyOS with Apple Studio Display repeatedly hotplugs at native 5120x2880@60. Debugfs showed the default link sometimes using HBR2 (`Current: 4 0x14 0`) even though the connector reported and verified HBR3 (`4 0x1e 16`). Writing HBR3 changed the active link to `Current: 4 0x1e 0`, matching the CachyOS forum workaround for avoiding the HBR2+DSC path (https://discuss.cachyos.org/t/fix-apple-studio-display-5k-flicker-black-screen-on-linux-with-amd-macbookpro16-1-by-forcing-displayport-hbr3-and-avoiding-dsc/29495), but later flickers still occurred while already at HBR3. On 2026-06-04, full USB4 resets renumbered the Studio Display connector from DP-7/DP-8 to DP-5/DP-6, so the service was changed to scan all DP-* debugfs link settings by default instead of hardcoding DP-7. This service is therefore a Framework-only stabilizer to keep the link from falling back to HBR2, not a proven full fix for the remaining HPD or USB4 drops. Related Framework report: https://community.frame.work/t/apple-studio-display-flickers-after-firmware-update-to-0-0-3-5/82587. Upstream/default direction: remove this service once the AMD/USB4/Studio Display path no longer falls back to HBR2 or once a kernel/firmware fix makes the debugfs preference unnecessary; validate by confirming the active Studio Display DP connector remains at HBR3 after reboot/hotplug without the service and by running `nix eval .#systemConfigs.cachyos-framework.drvPath --show-trace`.
- `ai-kitten-patch-pkgs-system-warning.nix` in `modules/home-manager`: target `homeConfigurations.cachyos-framework13`. Temporarily copies the upstream ai-kitten Home Manager module and replaces `self.packages.${pkgs.system}.ai-kitten` with `inputs.ai-kitten.packages.${pkgs.stdenv.hostPlatform.system}.ai-kitten` to avoid the deprecated `pkgs.system` evaluation warning. Upstream: private `git@github.com:AdrielVelazquez/aiKitten.git` main commit `12b10b525afc9d7f9895f610a6a82714a87b1680` still has the deprecated access in `flake.nix`. Checked 2026-07-10 with `rtk git ls-remote git@github.com:AdrielVelazquez/aiKitten.git refs/heads/main`: upstream `refs/heads/main` remains `12b10b525afc9d7f9895f610a6a82714a87b1680`. Next check: rerun `rtk git ls-remote git@github.com:AdrielVelazquez/aiKitten.git refs/heads/main`, update the `ai-kitten` flake input if it changed, and remove the local patch once upstream uses `pkgs.stdenv.hostPlatform.system`; validate with `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath --show-trace`.

Resolved pinned exceptions:

- 2026-05-27: removed `nixpkgs-kernel-master` from `flake.nix` for target `nixosConfigurations.razer14`. Original issue: the Razer host needed Linux 7.0.10 for the upstream MediaTek Bluetooth resume fix, while the temporary PR pin also supplied OpenRazer 3.12.3 to avoid the Linux 7.0.10 `hid_report_raw_event` build failure. Resolution: `nixos-unstable` provided `linux_7_0` 7.0.10, so `boot.kernelPackages` moved back to the main `nixpkgs` input. The later package-only OpenRazer and `linux_7_0` exceptions are resolved separately below; this entry is retained as the history of removing the broader secondary input. Validation used `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`.
- 2026-07-10: removed the separate `nixpkgs-antigravity` input. Original issue: the prior main Fleet pin lacked `pkgs.antigravity-cli`, requiring a second Nixpkgs package set for `homeConfigurations.adriel`, `homeConfigurations.cachyos-framework13`, and their embedded NixOS Home Manager configurations. Resolution: refreshed base `0bb7ec54c8483066ec9d7720e780a5caa71f8612` contains `antigravity-cli` 1.0.12 from merged PR #532344 / commit `3a4588b661380663fb1204cc4b6da1f106ddc6a4`, following initial PR #523246 / commit `f123c32d53beed2732137a65deb285db994c3aec` and binary-name fix PR #524076 / commit `db37039ed1f394f02b6c68605276930b0c73815c`. `modules/home-manager/antigravity-cli.nix` now uses the main `pkgs.antigravity-cli`, and the secondary lock node is gone. Validation: `nix eval --raw .#homeConfigurations.adriel.pkgs.antigravity-cli.version` returned `1.0.12`; `nix eval .#homeConfigurations.adriel.activationPackage.drvPath`, `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath`, `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath`, and `nix eval .#nixosConfigurations.dell.config.system.build.toplevel.drvPath` returned derivation paths.
- 2026-07-10: removed the Razer host's `linux_7_0` pin. Original issue: Linux 7.0.10 was selected for the MediaTek Bluetooth resume fix associated with Nixpkgs PR #523596 (`436d0dc2f2c1c949fcc575940ccf5f00b32d9424`), while the host still needed a compatible OpenRazer package. Resolution: the refreshed main package set no longer exposes the EOL `linux_7_0`; `boot.kernelPackages` now uses `pkgs.linuxPackages_latest`, verified at Linux 7.1.3. Affected output: `nixosConfigurations.razer14`. Validation: `nix eval --raw .#nixosConfigurations.razer14.config.boot.kernelPackages.kernel.version` returned `7.1.3`, and `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath` returned a derivation path.
- 2026-07-10: removed the local OpenRazer 3.12.3 source, kernel-module, and daemon overrides from `hosts/razer14/system-overrides.nix`. Original issue: the older pinned OpenRazer failed against Linux 7.0.10, so upstream OpenRazer `v3.12.3` and Nixpkgs commit `99643def59501d1eabb1ca01ef701b66d41908fe` were used locally. Resolution: refreshed Nixpkgs contains OpenRazer 3.12.4 from merged PR #539234 / commit `c4abbfa59497b232098087b63d627d87cb8e1cdf`; the normal `hardware.openrazer` feature settings remain, but package overrides are absent. Affected output: `nixosConfigurations.razer14`. Validation: `nix eval --raw .#nixosConfigurations.razer14.pkgs.openrazer-daemon.version` returned `3.12.4`, and `nix eval .#nixosConfigurations.razer14.config.system.build.toplevel.drvPath` returned a derivation path.
- 2026-07-10: removed `overlays/asdbctl-path-studio-display-1118.nix` and the handwritten system-manager udev rule. Original issue: Nixpkgs `asdbctl` 1.0.0 covered Apple product `05ac:1114` but not this Studio Display's `05ac:1118`, requiring local `asdbctl` 1.1.0 and rules for `1114`, `1116`, and `1118`. Resolution: Nixpkgs PR #527230 merged as commit `29d945b3ea4c3dc94150c6fffb32db8ec5e8fc79`; refreshed Nixpkgs packages `asdbctl` 1.1.0, and system-manager now sources `${pkgs.asdbctl}/lib/udev/rules.d/20-asd-backlight.rules`. Affected outputs: `homeConfigurations.cachyos-framework13`, `systemConfigs.cachyos-framework`, and configurations consuming the shared Niri brightness package behavior. Validation: `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.asdbctl.version` returned `1.1.0`; `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` and `nix eval .#systemConfigs.cachyos-framework.drvPath` returned derivation paths, and the built packaged rule contained product IDs `1114`, `1116`, and `1118`.
- 2026-07-10: removed `home.enableNixpkgsReleaseCheck = false` from `users/adriel-cachyos/default.nix`. Original issue: the mutable Fleet branch and the then-pinned Home Manager reported incompatible release generations, producing a mismatch warning for `homeConfigurations.cachyos-framework13`. Resolution: locked Home Manager commit `cee3f0e7fec85d80c149c7afc29926747f7bd3c2` and refreshed nixpkgs both report release 26.11, so the upstream release check remains enabled and no suppression is needed. Validation: `nix eval --raw .#homeConfigurations.cachyos-framework13.config.home.version.release` and `nix eval --raw .#homeConfigurations.cachyos-framework13.pkgs.lib.trivial.release` both returned `26.11`; `nix eval .#homeConfigurations.cachyos-framework13.activationPackage.drvPath` returned a derivation path without the release mismatch warning.
- 2026-07-10: removed the local Headroom 0.31.0 package, Home Manager proxy service, OpenCode routing overrides, and obsolete Codex transport patch history from active configuration. Original issue: Headroom was pinned outside Nixpkgs to compress AI-agent context, and earlier releases required a local Codex HTTP fallback because WebSocket Responses traffic bypassed compression. Upstream 0.31.0 removed the transport-patch need, but Framework measurements showed only a 4.6 percent whole-request proxy reduction while the service used about 1 GB RSS, roughly 82 percent of one CPU core, and repeatedly skipped Kompress work under ONNX saturation. Resolution: OpenCode now uses the LLM Platform plugin directly; all `local.headroom` options, service declarations, package derivations, routing headers, and Headroom environment variables are removed. Independent RTK command filtering remains enabled. Affected outputs: `homeConfigurations.adriel`, `homeConfigurations.cachyos-framework13`, `nixosConfigurations.razer14`, and `nixosConfigurations.dell`. Validation: all four exact output evaluations returned derivation paths, and tracked Nix configuration contains no `local.headroom`, `HEADROOM_`, `x-headroom-`, `headroom-ai`, or `headroom-proxy` references.
- 2026-07-10: removed `lib.mkForce` from `services.userborn.enable` in `hosts/cachyos-framework13-system-manager/configuration.nix`. Original issue: system-manager enabled userborn by default and a normal assignment could not override it, tracked in numtide/system-manager issue #350 (https://github.com/numtide/system-manager/issues/350). Resolution: the issue is closed and the current locked system-manager module accepts the ordinary `services.userborn.enable = false;`, so `lib` and the force workaround are unnecessary. Affected output: `systemConfigs.cachyos-framework`. Validation: `nix eval .#systemConfigs.cachyos-framework.drvPath` returned a derivation path, and `git grep -n 'services.userborn.enable = lib.mkForce false' -- '*.nix'` returned no matches.
- 2026-07-10: removed the direct `${inputs.nixpkgs}/nixos/modules/security/dhparams.nix` import from `parts/system-manager.nix`. Original issue: the system-manager integration previously depended on the NixOS dhparams module and imported it explicitly as a compatibility workaround for `systemConfigs.cachyos-framework`. Resolution: refreshed Nixpkgs removed `security.dhparams` in favor of RFC 7919 parameters, so the path no longer exists and the stale import itself blocked evaluation; current system-manager evaluates without it. Validation: `nix eval .#systemConfigs.cachyos-framework.drvPath` returned a derivation path, and `git grep -n 'nixos/modules/security/dhparams.nix' -- '*.nix'` returned no matches.
